This folder contains API module
How To run the API
In the API directory open the windows command prompt and run venv\Scripts\activate to activate the virtual environment
Everytime your doing anything on the API, make sure the virtual environment is activated
To install all dependencies do pip install -r requirements.txt (If it's your first time running the web)
Run the command flask run to start the API
To leave virtual environment just type deactivate.
